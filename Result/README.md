## Result/

The `Result/` folder contains `.rda` files storing simulation outputs generated by the simulation scripts. These files include the results for the following settings:
- functional completion,
- functional clustering,
- functional regression, and
- factor models.

The saved `.rda` objects are generated by `Simulation_result.R` to aggregate results and reproduce the simulation summaries and figures reported in the manuscript.